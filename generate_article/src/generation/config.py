# Настройки RAG-генератора статей

RAG_CONFIG = {
    'llm_model': 'qwen2.5:0.5b',  # или путь к локальной модели
    'embedding_model': 'sentence-transformers/all-MiniLM-L6-v2',
    'max_new_tokens': 1200,
    'temperature': 0.7,
    'top_p': 0.9,
    'repetition_penalty': 1.1,
    'article_length_range': (300, 800),
    'generated_article_similarity': 0.8
}

# Промпт для генерации статьи
PROMPT_TEMPLATE = """На основе следующих актуальных статей о тренде "{cluster_name}" сгенерируй новую уникальную статью.

Контекст статей (отсортированы по релевантности):
{context}

Требования к статье:
1. Язык: исключительно русский.
2. Заголовок должен чётко отражать суть тренда.
3. Содержание должно быть структурированным: введение, основная часть, заключение.
4. Используй ключевые идеи из контекста, но не копируй дословно.
5. Объём: {min_words}–{max_words} слов.
6. Стиль: информативный, журналистский, без жаргона.
7. В конце добавь краткий вывод (1–2 предложения).

Сгенерируй заголовок и содержание новой статьи. Формат вывода:
Заголовок: [заголовок]
Содержание: [содержание]"""
