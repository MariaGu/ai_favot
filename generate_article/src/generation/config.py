# Настройки RAG-генератора статей
RAG_CONFIG = {
    'top_cluster_rank': 1,
    'max_articles_for_rag': 5,
    'min_similarity_threshold': 0.6,
    'generated_article_similarity': 0.85,
    'article_length_range': (300, 600),
    'embedding_model': 'all-MiniLM-L6-v2',
    'llm_model': 'microsoft/Phi-3-mini-4k-instruct',
    'device': 'auto',

    # Параметры генерации текста
    'max_new_tokens': 1200,
    'temperature': 0.7,
    'top_p': 0.9,
    'repetition_penalty': 1.1
}

# Промпт для генерации статьи
PROMPT_TEMPLATE = """На основе следующих актуальных статей о тренде "{cluster_name}" сгенерируй новую уникальную статью.

Контекст статей (отсортированы по релевантности):
{context}

Требования к статье:
1. Заголовок должен чётко отражать суть тренда.
2. Содержание должно быть структурированным: введение, основная часть, заключение.
3. Используй ключевые идеи из контекста, но не копируй дословно.
4. Объём: {min_words}–{max_words} слов.
5. Стиль: информативный, журналистский, без жаргона.
6. В конце добавь краткий вывод (1–2 предложения).

Сгенерируй заголовок и содержание новой статьи. Формат вывода:
Заголовок: [заголовок]
Содержание: [содержание]"""
